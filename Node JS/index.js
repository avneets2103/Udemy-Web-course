const sp = require("superheroes");
var superhero_name = sp.random();
console.log(superhero_name);

// initialize a npm package
// install the nmp package we need 
// get in the code and use

const sv = require("supervillains");
var supervillain_name = sv.all;
console.log(supervillain_name);